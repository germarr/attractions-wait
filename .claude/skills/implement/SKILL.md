---
description: Execute the implementation plan by processing all tasks in docs/issues.
---


## Outline

1. Explore `docs/issues` to get the issues directory path.

2. **Load context**: `CONTEXT.md` and `docs/adr` and `docs/prd`


3. **Execute tasks** in order:
   - Complete each issue before moving to the next
   - Halt on failure and report the issue

4. **Validate**: Verify all tasks are completed and the implementation matches the spec.